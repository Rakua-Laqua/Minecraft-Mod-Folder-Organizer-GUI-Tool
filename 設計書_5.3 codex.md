# Minecraft Mod JAR抽出・lang整理GUIツール 設計書 v2.1

最終更新: 2026-02-25  
対象実装: `ModLangOrganizer`（WPF / .NET 8）

## 1. 目的
- 親フォルダ直下の Minecraft Mod `*.jar` を対象に、安全に展開して lang ファイルを抽出する。
- 抽出結果を `jar` 単位で整理し、競合時は既存を壊さず競合コピーで退避する。
- スキャン（事前計画表示）と実行を分離し、誤操作しにくい GUI として提供する。
- 任意で lang フォールバック（例: `en_us` -> `ja_jp` 自動生成）を適用する。

## 2. 対象環境
- OS: Windows 10/11
- ランタイム: .NET 8 Desktop Runtime
- GUI: WPF（MVVM）
- 入力: 親フォルダ直下に並ぶ `*.jar` ファイル
- 出力ルート既定: `TargetDir`（親フォルダ）

## 3. 用語
- `TargetDir`: Mod `*.jar` が直下に並ぶ親フォルダ
- `OutputRoot`: 抽出結果の出力ルート（既定は `TargetDir`）
- `jarRootName`: `jar` 拡張子を除いたファイル名
- `WorkDir`: 1つの `jar` を一時展開するディレクトリ（例: `%TEMP%\mod-organizer\<jarRootName>_<hash>`）
- `lang候補`: `assets/<modid>/lang` に一致する候補
- `出力先（単一候補）`: `<OutputRoot>/<jarRootName>/`
- `出力先（複数候補）`: `<OutputRoot>/<jarRootName>/<modid>/`
- `競合`: 出力先に同名ファイルが既に存在する状態
- `langフォールバック`: ターゲット名ファイルが無い場合、ソース名ファイルから拡張子を保ってコピー生成する処理

## 4. 機能要件

### 4.1 フォルダ選択・設定
- `TargetDir` をユーザーが選択できる（フォルダピッカー）。
- `OutputRoot` は任意変更可能。
- 「親フォルダと同じ場所に出力する」ON時は `OutputRoot = TargetDir` とし、手動変更を無効化する。
- `TargetDir` が存在しない場合、スキャン/実行を不可にする。

### 4.2 解析（スキャン）
各 `*.jar` に対し以下を判定し、一覧に表示する。
- `jar` 読取可否（破損判定）
- `assets/<modid>/lang/` 候補数（0/1/複数）
- 処理方針（A: langあり / B: langなし）
- 予定操作件数（`Extract/CreateDir/Copy/ConflictCopy/Cleanup/Skip`）
- 実行前整合性チェック用スナップショット（ファイル名、サイズ、最終更新時刻）

補足:
- スキャン時点では実ファイル操作を行わない（ドライラン）。
- `langフォールバック` は実行時処理であり、スキャン予定操作には含めない。

### 4.3 実行（jar抽出 + lang集約）
対象: `TargetDir` 直下の各 `*.jar`

#### A) lang候補が見つかった場合
1. `jar` を `WorkDir` へ安全展開する（Zip Slip対策）。
2. 出力先を決定する。
   - 候補が1件: `<OutputRoot>/<jarRootName>/`
   - 候補が複数: `<OutputRoot>/<jarRootName>/<modid>/`
3. `lang` 配下の全ファイルを相対パス維持で再帰コピーする（`lang` ディレクトリ自体は作らない）。
4. 競合時はファイル内容を比較して処理する。
   - 同一内容: スキップ
   - 異なる内容: 競合コピーとして退避
5. 競合コピー名は `*.conflict.<sourceTag>.<n>.*` 形式で連番生成する。
   - 例: `en_us.conflict.example_mod.1.json`
6. `langフォールバック` 有効時:
   - 出力先直下（再帰なし）から `sourceName.*` を探索
   - `targetName.*` が無ければコピー生成（拡張子は維持）
7. 処理後に `WorkDir` を削除（Cleanup）。

#### B) lang候補が無い / `jar` 破損
- 該当 `jar` はスキップとして記録する。
- 可能な範囲で後続 `jar` の処理を継続する。

#### C) 1つのjarに複数lang候補がある場合
- 全候補を処理対象とする（選択式にしない）。
- 1つの `jarRootName` 配下にまとめ、`<modid>` を切って分離する。

### 4.4 実行前整合性チェック
- スキャン時スナップショットと実行直前スナップショットを比較する。
- 差分がある `jar` が1件でもあれば実行を中断し、再スキャンを要求する。
- `FileSystemWatcher` で `*.jar` の変更を監視し、変更検出時に「再スキャン必要」状態へ遷移させる。

### 4.5 進捗・状態表示
- スキャン進捗: `current/total`、対象 `jar` 名、進捗率。
- 実行進捗: `Started/Completed` イベントを jar単位で反映。
- 実行完了時にサマリ表示:
  - 成功 / 警告 / スキップ / 失敗 / Cleanup失敗

### 4.6 ログ・出力
- 画面内ログ（時刻/レベル/メッセージ）を保持する。
- ログは任意パスに `.txt` 保存できる。
- 実行時は出力先パス・競合コピー・フォールバック実行有無をログに残す。

### 4.7 安全対策
- 実行確認ダイアログを必須にする。
- 既存出力を破壊的に削除しない（競合時は退避）。
- Cleanup 対象は `WorkDir` のみ。
- 任意で実行前バックアップ Zip を作成する。
  - 出力先: `TargetDir` の親ディレクトリ
  - ファイル名: `<TargetDir名>_backup_<timestamp>.zip`

### 4.8 セキュリティ要件
- Zip Slip対策:
  - 展開先正規化パスが許可ルート配下かを検証
  - `..` を含むエントリを拒否
- 再帰列挙/削除で reparse point（symlink/junction）を辿らない。
- 一時展開フォルダは `jar名 + ハッシュ` で衝突を避ける。

## 5. 非機能要件
- UIをブロックしない（バックグラウンド処理 + 進捗通知）。
- `jar` 単位で例外を隔離し、可能な範囲で継続する。
- キャンセル粒度:
  - 既定: `jar` 単位
  - 任意: ファイル単位
- 設定永続化:
  - `settings.json` を `%LocalAppData%/ModLangOrganizer` に保存
  - デバウンス保存 + 直列化 + `.tmp -> Replace/Move` のアトミック書込
  - 破損JSON検出時は `settings.broken.<timestamp>.json` に退避

## 6. UI設計（単一ウィンドウ）

### 6.1 入力エリア
- 親フォルダ選択 (`TargetDir`)
- 出力ルート (`OutputRoot`)
- 「親フォルダと同じ場所に出力する」チェック

### 6.2 オプションエリア
- 実行前バックアップ（Zip）
- キャンセル粒度（jar単位 / ファイル単位）
- langフォールバック
  - 有効/無効
  - コピー元ファイル名（拡張子なし）
  - コピー先ファイル名（拡張子なし）

### 6.3 実行エリア
- ボタン: `[スキャン] [実行] [キャンセル]`
- スキャン中/実行中の進捗バー表示
- 再スキャン警告バナー表示（jar変更検出時）

### 6.4 スキャン結果テーブル
- Mod名（jarファイル名）
- 健全性（OK/破損）
- lang候補数
- 方針（A/B）
- 予定操作サマリ（`E/D/C/CF/CL/S`）
- スナップショット状態
- ステータス（未処理/処理中/成功/警告/スキップ/失敗）

### 6.5 ログビュー
- 画面内ログ一覧
- ログ保存ボタン
- 件数バッジ

## 7. 処理仕様（アルゴリズム）

### 7.1 jar列挙
- `Directory.GetFiles(TargetDir, "*.jar", TopDirectoryOnly)` で列挙し、名前順で処理する。

### 7.2 スキャン
- `jar` を展開せずアーカイブエントリ一覧を取得する。
- 正規表現 `^assets/([^/]+)/lang/` で候補検出。
- 候補ごとに planned operations を生成する。

### 7.3 安全展開
- `WorkDir` を作成し、Zip Slip対策付きで展開する。

### 7.4 出力先決定
- `jarRootName = Path.GetFileNameWithoutExtension(jar)`
- 候補数1件なら `Out = <OutputRoot>/<jarRootName>/`
- 候補数複数なら `Out = <OutputRoot>/<jarRootName>/<modid>/`

### 7.5 コピー・競合処理
- `SrcLang` 配下を再帰列挙し、相対パスで `Out` へコピー。
- 既存時はバイト比較。
  - 同一: スキップ
  - 相違: `ConflictResolver` で競合名を生成し退避コピー

### 7.6 langフォールバック
- 条件: `LangFallbackEnabled` かつ source/target が空でない。
- `Out` 直下の `sourceName.*` を探索。
- `targetName.*` が存在しない場合のみコピー生成。

### 7.7 Cleanup
- `WorkDir` を再帰削除（リンク先非追従）。
- Cleanup失敗は警告カウントへ反映する。

### 7.8 実行前検証
- スナップショット差分を検出した場合は実行中止して再スキャン要求。

### 7.9 キャンセル
- `PerFile`: ファイルループ節目でトークン確認。
- `PerJar`: jar完了単位でトークン確認。

### 7.10 設定保存
- 起動時ロード、変更時スケジュール保存、終了時 Flush する。

## 8. エラー処理
- `jar` 単位で例外隔離。
- 代表的な失敗:
  - 壊れた `jar`
  - 展開時の不正エントリ（Zip Slip）
  - コピー失敗（権限/ロック）
  - Cleanup失敗
- 失敗時はログ出力し、可能な限り次の `jar` へ継続する。

## 9. 実装方針（アーキテクチャ）
- パターン: MVVM
- 層構成:
  - UI層（WPF XAML）
  - ViewModel層（状態管理/コマンド）
  - Domain層（Scan/Execute/Validate）
  - Infrastructure層（ファイル/Zip/ログ/設定永続化）

### 9.1 主要クラス
- `MainViewModel`
  - 主プロパティ: `TargetDir`, `OutputRoot`, `OutputRootSameAsTarget`, `BackupZip`,
    `CancelGranularity`, `LangFallbackEnabled`, `LangFallbackSourceName`, `LangFallbackTargetName`
  - Commands: `BrowseFolderCommand`, `BrowseOutputCommand`, `ScanCommand`, `ExecuteCommand`,
    `CancelCommand`, `SaveLogCommand`
  - `FileSystemWatcher` による jar 変更監視

- `JarScanner`
  - `EnumerateJars(targetDir)`
  - `ScanJar(jarPath, outputRoot)`（lang候補検出 + planned operations 構築）

- `Executor`
  - `ExecuteAsync(scanResults, outputRoot, options, progress, ct)`
  - `CreateBackupAsync(targetDir, ct)`
  - `ApplyLangFallback(outDir, options, logPath)`（内部）

- `ConflictResolver`
  - `BuildConflictName(baseName, sourceTag, destDir)`

- `SnapshotValidator`
  - `Validate(scanResults)`（差分jar名一覧）

- `FileSystemService`
  - `EnsureDir`, `CopyFile`, `DeleteRecursiveNoFollow`,
    `EnumerateFilesNoFollow`, `EnumerateDirectoriesNoFollow`, `IsSameContent`

- `ArchiveExtractor`
  - `DetermineWorkDir(jarPath)`
  - `ExtractSecure(jarPath, destDir, ct)`
  - `ListEntries(jarPath)`

- `SettingsService` / `SettingsStore`
  - 設定ロード、デバウンス保存、アトミック保存、破損退避

- `Logger`
  - `Info/Warn/Error`, `ExportAsync`
  - UIスレッド（Dispatcher）で `ObservableCollection` を更新

## 10. Git運用上の注意
- 競合コピー（`*.conflict.*`）は差分確認後にコミットする。
- 大量生成される抽出結果を追跡対象にするかは `.gitignore` で明確化する。

## 11. テスト観点
- 正常系:
  - 単一lang候補: `<OutputRoot>/<jarRootName>/...` に出力される
  - 複数lang候補: `<OutputRoot>/<jarRootName>/<modid>/...` に分離される
  - 同一内容競合: スキップ
  - 異なる内容競合: 競合コピー生成
  - langフォールバック: `targetName.*` 不在時のみ生成
- 異常系:
  - 壊れた `jar` はスキップ
  - Zip Slip疑いエントリは展開拒否
  - 権限/ロック/Cleanup失敗の警告化
  - 破損 `settings.json` の退避と既定値復帰
- 整合性:
  - スキャン後 `jar` 変更時に実行拒否
  - planned operations 件数と実行ログの整合性

## 12. 配布
- self-contained / framework-dependent の両方式を想定する。

## 13. 今後の拡張候補
- スキャン段階で `FallbackCopy` 件数予測を表示
- フォールバック対象を再帰対応するオプション
- 除外ルール（パターン指定）と `README`/メタファイル保護強化
- 自動テスト（単体 / E2E）整備
