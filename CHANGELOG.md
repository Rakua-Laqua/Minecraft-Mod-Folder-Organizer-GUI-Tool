# CHANGELOG

## [3.2.3] - 2026-09-11
デプロイ処理のトランザクション化と自動ロールバック機構を導入し、中断・障害時の配置先保護と整合性担保を強化。

### Changed
- **デプロイ処理のトランザクション・安全ロールバック強化**:
  - 反映先フォルダ内に一時ステージングディレクトリ（`.modmanager-transaction-*`）を確保し、ファイル配置・削除をリネーム（Move）ベースでアトミックに実行する構造へ改善
  - デプロイ中の例外発生やユーザーキャンセル時に、配置・更新・削除されたファイルを逆順で元の状態へ自動復元するロールバック処理を導入。万が一の復元失敗時にも復旧用データを保持し、エラーメッセージでパスを案内
  - Windows API (`ReplaceFileW`) の置換失敗に伴うマニフェストファイル消失エッジケースに備え、事前バックアップ退避と安全な復元機構を実装
- **ファイル同一性判定の厳密化**:
  - ファイルサイズと更新日時の概算比較から `FileSystemService.IsSameContent`（実データ内容比較）へ変更。タイムスタンプの差異による不要な再デプロイを抑制しつつ、同サイズ・同日時の内容変更を確実に検出
- **リソースパック配置のアトミック性向上**:
  - `ResourcePackBuilder` のフォルダ移動失敗時におけるファイル単位フォールバックを廃止し、フォルダが二重に分散破損するリスクを排除。移動失敗時は常にバックアップから安全に復元する方針に一本化

### Added
- **デプロイおよびマニフェスト安全性の回帰テスト拡充**:
  - ステージング失敗、コミット中ロック、マニフェスト置換障害（消失・リネーム）、キャンセル時の完全ロールバック、復元失敗時のデータ保持を検証する自動テスト群（`DeploymentSafetyTests`, `ManifestSafetyTests`）を追加

## [3.2.2] - 2026-09-08
lang抽出時のJAR全展開を廃止し、必要な翻訳ファイルだけを一時抽出することでディスクI/OとExplorer負荷を削減。

### Changed
- **lang抽出の部分展開化**: JAR内の全ファイルをTEMPへ展開する方式をやめ、`lang` 直下の `.json` / `.lang` だけを一時抽出するよう変更。class・texture・model等の無関係ファイルの大量生成と削除を避け、抽出時のストレージ負荷を大幅に抑制
- **JAR構造・mapping互換性の維持**: JAR構造の検出は従来通り展開不要のエントリ一覧スキャンで行い、`LangCandidate` と `%LOCALAPPDATA%\ModLangOrganizer\workspaces\<workspaceId>\mapping.json` のアーカイブ内パス管理も変更しないため、既存の抽出先・再マッピング・JAR反映フローとの互換性を維持
- **安全検証を維持したI/O削減**: Zip Slipおよび `..` パスの検証は抽出対象外を含む全ファイルエントリに対して継続し、安全性を落とさず実際のファイル書き込みだけをlang対象へ限定

## [3.2.1] - 2026-09-08
### Fixed
- **起動時クラッシュの修正（XAML例外・バインディング競合の解消）**:
  - `ModDeployView.xaml` において、`GradientStop.Color` に `SolidColorBrush` 型リソース（`SuccessBrush`）を指定していた型不一致例外を解消し、Color型リソース（`ColorSuccess`）に修正
  - `ProgressBar.Value` の暗黙 TwoWay バインディングと `DeployViewModel.DeployProgressPercent`（読み取り専用）の競合による初期化時例外を解消（`Mode=OneWay` 指定および public setter への緩和）
  - アクティビティログ枠で未定義だった `TerminalPanel` スタイルを `Theme.xaml` に新規定義（`BgDeepBrush`・枠線・角丸）
- **タブ切り替え不具合の修正（デプロイ画面の常時表示解消）**:
  - `MainWindow.xaml` で `ModDeployView` 自体に `DataContext="{Binding Deploy}"` を指定していたため、`Visibility` が親コンテキストの `SelectedNavigationTab` を参照できずバインディングエラー（常に Visible）となっていた構造を修正。親コンテキストを維持するラッパー `Grid` で囲むことで、STEP 1 / STEP 2 が正常に切り替わるよう修正
- **UI表示崩れ・視認性の改善**:
  - **TreeViewの非フォーカス時白光り解消**: アイテム選択後に別領域をクリックした際、Windows既定の `InactiveSelectionHighlightBrush`（白系）が適用されていた問題を、ダークテーマ調和の半透明アクセント色（`#1A6C63FF`）でオーバーライドして白浮きを解消
  - **差分詳細表示ボタンのつぶれ解消**: ボタンの Height / Padding を調整して上下テキストの切れを解消し、開閉状態に応じて「▼ 差分ファイル詳細を表示」/「▲ 差分ファイル詳細を折りたたむ」と動的に切り替わるよう改善
  - **差分テーブル（DataGrid）の白背景・白飛び改善**: `ModernDataGrid` スタイル群を適用し、Windows標準の白背景行をダークグレーに統一。ファイル名（白）、サイズ（等幅・淡青）、詳細備考（淡グレー）をくっきり視認できるよう配色を改善

## [3.2.0] - 2026-09-08
### Added
- **MOD構成デプロイの実機能化（WPF/C# ネイティブ完全統合）**:
  - `Minecraft copy maneger`（Python版）の仕様を C# / WPF へ完全移植し、UIモックから実動作へ置き換え
  - **MOD倉庫スキャナー (`ModRepositoryScanner`)**:
    - 管理元フォルダ配下のJARファイルを再帰スキャンし、カテゴリ構造を反映した階層ツリーを構築
    - `_backup`、`有効化しないmod`、`無効化`、`disabled`、`bak` などの無効化フォルダを自動検出し、ツリー上で視覚的に区別
  - **安全デプロイサービス (`ModDeploymentService`)**:
    - **安全な同期モード (Sync)**: 反映先（Minecraftの `mods` フォルダ）に `.modmanager.json` マニフェストを自動生成・記録。選択解除されたMODのうち本ツールが配置したファイルのみを安全に削除整理し、**手動導入のMODや他ツール管理のMODは絶対に削除しない**保護を実現
    - **追加モード (Add)**: 既存ファイルを削除せず、選択したMODのみを追加・上書きコピー
    - **同名MODの衝突検出 (Conflict Detection)**: 異なるサブフォルダから同名JARが重複選択された場合、デプロイ前に警告バナーを表示して安全にブロック
    - **リアルタイム差分計算**: 新規・更新・削除・維持のファイル件数を即座に算出し、「▼ 差分ファイル詳細を表示」パネルで対象一覧（元パス、サイズ、理由）を完全可視化
    - **自動バックアップ & 安全ロールバック**: 反映実行時に更新・削除対象ファイルを `.modmanager_backup/` 配下へ自動退避
  - **プロファイル管理基盤 (`ModProfileStore`)**:
    - 現在のMOD選択状態に名前をつけてローカル（`%LOCALAPPDATA%\ModLangOrganizer\deploy-profiles.json`）に保存・ワンクリック呼び出し・削除
  - **デプロイ設定の自動永続化 (`ModDeploySettingsStore`)**:
    - 管理元フォルダパス、反映先modsパス、デプロイモード、バックアップ有無、最後に選択していたMOD一覧などを `%LOCALAPPDATA%\ModLangOrganizer\deploy-settings.json` に自動保存・次回起動時に復元
  - **非同期実行 & UI進捗同期**:
    - バックグラウンド実行（`Task.Run`）、パーセント付きプログレスバー、アクティビティログ表示、キャンセル対応

### Removed
- **旧スタンドアロン Python ツールの削除**:
  - C# / WPF 側へ MOD構成デプロイ機能が完全統合・実動化されたことに伴い、移行元であったスタンドアロン版 Python ツール（`Minecraft copy maneger/` 配下）を削除・一本化

## [3.1.0] - 2026-09-08
### Added
- **MOD構成デプロイ機能（STEP 2）の追加および統合スタジオ化**:
  - メインウィンドウ最上部にナビゲーションタブバーを新設し、「STEP 1: MOD言語・翻訳抽出」と「STEP 2: MOD構成デプロイ」を切り替え可能な統合スタジオへUIを拡張
- **MOD構成デプロイUI（Copy Manager画面）の追加**:
  - 三態チェックボックス対応の階層ツリービュー（Tri-state TreeView）
  - フォルダ単位の一括チェック/解除、個別JARチェック、親ノードの半選択状態（Indeterminate）対応
  - 「全選択」「全解除」「選択反転」「すべて展開」「すべて折りたたむ」の一括操作ツールバー
  - リアルタイム検索フィルターおよび無効フォルダ表示切替トグル
  - デプロイ操作パネル（同期モード/追加モード、バックアップトグル、差分サマリー、プロファイル操作バー）
- **ドメインモデルの追加**:
  - `ModTreeNodeModel`, `DeployPlanModel`, `ModProfileModel`, `DeploySettingsModel` 等のデプロイ関連モデルを新設
- **スタンドアロン版「Minecraft copy maneger」の統合**:
  - Python (PySide6) ベースの単体GUIツールを `Minecraft copy maneger/` 配下に統合し、`run.bat` からの即時単体起動に対応

## v3.0.1 - 2026-09-07
### Fixed
- **大容量言語ファイルのJAR反映時における検証失敗（誤判定）の修正**:
  - `JarArchiveUpdater` のストリームバイト比較処理（`StreamsEqual`）において、ZIP解凍ストリーム（`DeflateStream`）の仕様である分割読み取り（Partial Read）に対応。要求バッファサイズを満たすまで確実に読み切る `ReadFullChunk` を導入
  - 数万バイト〜100KBを超える大きな言語ファイル（`de_de.json`, `ru_ru.json`, `en_us.json`, 翻訳済み `ja_jp.json` 等）を含むMODで「反映後JARの検証に失敗しました」という例外が発生しロールバックされる不具合を根本解消
- **ファイル比較の堅牢性向上**:
  - `FileSystemService.IsSameContent` においても分割読み取りに対応し、特殊なファイルシステムや大容量ファイルでの比較信頼性を向上
- **回帰テスト拡充**:
  - 100KB超の言語ファイルを用いた追加・更新・未変更判定の自動テスト（`R6.LargeLangFileJarArchiveUpdate`）を追加

## v3.0.0 - 2026-09-05
### Added
- **2ペイン・ダッシュボード型ワークスペースへのUI全面刷新**:
  - 左サイドバー（幅340px）にフォルダ設定、各種オプション、主要アクションボタンを集約し、操作動線を最適化
  - 右メインエリアに広大な作業スペースを確保し、ウィンドウサイズに連動して伸縮するフルハイトテーブルを配置
- **KPIサマリーカード（4枚）**:
  - スキャン完了時に「総Mod数」「抽出可能 (langあり)」「フォールバック対象」「異常・破損」を色分けカードで即座に視認可能
- **リアルタイム検索＆フィルタータブバー**:
  - Mod名や言語コードによるリアルタイムインクリメンタル検索
  - 「すべて」「抽出可能」「要フォールバック」「異常」のクイックフィルタータブ（件数バッジ付き）
  - 全選択 / 全解除の一括操作ボタン
- **Mod一覧テーブルの表現力向上**:
  - 検出言語を `[en_us] [ja_jp]` のようなチップバッジで明瞭に一覧表示
  - 従来の暗号的な略語（`E:1 D:0...`）から「抽出: 1件 / フォールバック: 1件」などの自然な日本語表示へ改善
  - 各行に個別選択用チェックボックスを新設
- **ドッキング式ログターミナル**:
  - メインエリア下部にGridSplitterで高さを自在に調節できるコンソールパネルを配置

## v2.4.2 - 2026-09-05
### Fixed
- **MOD更新候補が複数存在する場合の安全スキップ**: MODのバージョンアップ等で旧JARが削除され、同一ModIdを持つ新JARの候補が2件以上存在する場合（例: 異なるバージョンや派生JARが複数置かれた場合）、先頭を誤採用せず自動更新を安全にスキップして Warning ログを出力するように修正（一致1件のみ自動更新）
- **旧mapping移行時におけるOutputRoot安全分離**: v2.4.0旧形式（TargetRoot単体）の mapping.json からの自動引き継ぎを、現在 `OutputRoot == TargetRoot`（既定値）の場合のみに限定。カスタムOutputRootが指定されている場合は旧設定を誤適用せず独立して新規管理するように修正
- **Fallback無効時や手動配置の既存翻訳ファイルに対するmapping救済登録**: `LangFallbackEnabled`（フォールバックコピー）のON/OFFに関わらず、抽出実行時に各Modの編集フォルダ直下に既に存在する未登録の `.json` / `.lang`（手動作成した `ja_jp.json` 等）を、ファイル内容を一切変更することなく安全に `mapping.json` へ登録し、次回JAR反映から漏れないよう救済
- **JAR反映時の Candidate ModId 厳格照合**: `JarLangImporter` で mapping の `ArchivePath` を検証する際、単にアーカイブ内の lang パスと照合するだけでなく、該当 Candidate の `ModId` が mapping エントリの `ModId` と完全に一致していることをセットで検証するように強化

## v2.4.1 - 2026-09-05
### Fixed
- **推測反映フォールバックの完全廃止**: mapping が存在しない・空の状態で「JARへ反映」を実行した際に従来の推測フォルダ探索へ逆戻りする問題を解消。mapping を唯一の正解とし、未関連付け状態での推測書き込みを完全に遮断して抽出を促す案内を表示するように改善
- **mapping エントリの防御的パスバリデーション強化**:
  - `EditPath` のパストラバーサル（`../` による出力ルート外参照）を遮断
  - `JarRelativePath` の完全一致のみを採用し、別ディレクトリの同名JARへの誤爆を防止
  - `ArchivePath` が該当 JAR の検出済み `ArchiveLangPath` 配下に収まっているかを検証し、無効パスや不正エントリの混入を遮断
  - `.json` / `.lang` 以外の非対応拡張子ファイルを安全に除外
- **既存 fallback 生成済みファイル（ja_jp.json 等）の mapping 登録漏れ解消**: JAR に存在せず手動翻訳や過去生成で出力フォルダに既に存在していた翻訳ファイルについて、再抽出時に mapping 登録をスキップしてしまい次回 JAR 反映から脱落する不具合を修正
- **MOD更新追従条件の厳格化**: 同一 ModId の候補 JAR が1件のみの場合でも、新 JAR 内に該当 `ArchivePath` に合致する有効な `ArchiveLangPath` が存在することを必須条件とし、lang 構造が異なる新バージョンへの誤付け替えを防止
- **TargetRoot と OutputRoot のペアによる workspace 安全分離**: ワークスペースID（SHA-256）の生成キーおよび `WorkspaceMapping` に `OutputRoot` を紐付け。同一 MOD フォルダに対して異なる lang 出力先を指定した場合でも、別個の独立した mapping として管理され誤反映を防止
- **確認ダイアログ文言の是正**: 「元フォルダの相対構造: 保持」という旧文言を、現在の実態に合わせて「編集フォルダ: Mod IDごとに整理（既存の抽出フォルダは維持）」および「反映基準: mapping.json に基づく正確なJAR内パス」に修正

## v2.4.0 - 2026-09-05
### Added
- **永続 mapping.json の導入**: 翻訳編集ファイルと元JARのアーカイブ内パスを1対1で記録・管理するマッピング機能を追加（`%LOCALAPPDATA%\ModLangOrganizer\workspaces\<workspaceId>\mapping.json`）。アトミック書き込みおよび `.bak` 自動バックアップに対応
- **直感的な簡略フォルダ構造**: 新規抽出時、冗長な `assets/` や JAR ファイル名階層を排除し、直感的な `<OutputRoot>/<modid>/<filename>`（例: `carbonconfig/ja_jp.json`）で配置
- **MOD更新への自動追従（スマート再マッピング）**: スキャン時、MODのバージョンアップ等で旧JARが消え同一ModIdを持つ新JARが検出された場合、mapping.json の参照先を自動更新。編集フォルダ側のパスを変更することなく新バージョンへの翻訳反映が可能
- **リソースパック出力先の永続化**: UIからリソースパック保存先を指定可能とし、設定（`AppSettings.ResourcePackOutputRoot`）に記憶。毎回フォルダ選択ダイアログを開く手間を削減

### Changed
- **JAR反映の mapping 基準化**: フォルダ構造からの推測反映を廃止し、`mapping.json` を唯一の正解として正確な元JARアーカイブ内パスへ反映。未知の無関係ファイルが誤ってJARに投入される事故を防止
- **既存フォルダ構造の保護**: 既に旧形式等で抽出済みのフォルダが存在する場合、強制的な移動・削除を行わず既存パスを mapping に自動登録してそのまま維持
- **UIボタン配置・表記の整理**:
  - メインフローが直感的に把握できるよう「スキャン」→「langを抽出」→「JARへ反映」の順にボタンを整理
  - 「リソースパック出力（推奨）」から「（推奨）」表記を削除し、JAR改変を避けたい場合の安全な代替手段として位置付けを明確化

## v2.3.2 - 2026-09-05
### Fixed
- **リソースパック置換時のロールバック安全性向上**: 置換失敗かつロールバックにも失敗した場合に旧バックアップデータが `finally` で削除されてしまう脆弱性を解消。バックアップは本番配置が正常完了した後にのみ削除し、万一のロールバック失敗時でも旧データを確実に残して例外メッセージにパスを明示するように修正
- **リソースパック `pack_format` バージョン対応表の是正**: Minecraft Wiki仕様に準拠し、`format 1`（1.6.1 - 1.8.9）および `format 2`（1.9 - 1.10.2）を追加、`format 3` の対象バージョンを `1.11 - 1.12.2` に正確に訂正
- **JAR Manifestパーサーのヘッダー名判定を厳密化**: 単なる部分一致ではなく、コロンより前の属性ヘッダー名が `-Digest` で終わるか判定するように精密化

## v2.3.1 - 2026-09-04
### Fixed
- **署名保護の精密化**: 同一内容の翻訳ファイル反映（no-op更新）の場合は署名ダイジェストを破壊しないためブロック対象外とし、実際に内容が改変されるエントリのみを厳密にブロックするよう修正
- **JAR Manifestパーサーの仕様準拠**: 72バイト制限による継続行（先頭スペース/タブ）の連結に対応し、長いパスを持つ署名対象ファイルを確実に見落とさず検出できるよう修正。また `*-Digest:` 属性が存在するセクションのみを署名対象として判定し過剰ブロックを防止
- **ZIP互換性バリデーションの強化**: Local Headerの順次走査から、アーカイブ末尾の Central Directory（中央ディレクトリ）と各 Local Header をピンポイントで直接走査する方式へ刷新。Data Descriptor付きエントリに遭遇してもシグネチャ探索の誤判定を起こさず、Java `ZipInputStream` でのクラッシュ条件（`STORED + bit3`）を確実に検証
- **リソースパックの旧ファイル残留防止**: 一時フォルダで完全構築したのちアトミックに置換する方式に変更し、前回出力後にMOD構成から削除・除外された古い翻訳ファイルがフォルダ内に残存しないよう修正
- **リソースパック標準パス判定**: Minecraft標準の `assets/<namespace>/lang/...` 構造のみをリソースパックへ出力し、非標準パス（`.../lang`）は自動スキップして警告ログおよび完了画面に通知する機能を追加
- **リソースパック出力のキャンセル対応**: 大量ファイルのコピー中も `CancellationToken` を監視し、キャンセルボタン押下時に安全かつ即座に中断・一時ファイル清掃を行うよう修正

### Added
- **リソースパック形式（pack_format）選択**: UIオプションにMinecraftバージョン別の `pack_format` 選択コンボボックスを追加（1.20.1: format 15 を既定とし、1.6.1〜1.21.4 まで自由に切り替え可能）

## v2.3.0 - 2026-09-04
### Added
- 翻訳ファイルをMinecraftリソースパックとして自動構築する「リソースパック出力（推奨）」機能を追加（MODのJARを一切改変しないため、ZIP破損や署名検証エラーのリスクがゼロ）
- UIに「📦 リソースパック出力（推奨）」ボタンを追加し、出力先として `resourcepacks` フォルダを自動提案
- 署名付きJARの署名対象ファイル（MANIFEST.MFや.SFにダイジェストがあるエントリ）に対する改変をブロックし、改ざん検知によるクラッシュを防止する安全保護を追加
- JAR出力直後にZIPバイナリヘッダーを走査し、Java `ZipInputStream` で破損と判定される不正エントリ（`STORED` かつ `Data Descriptor bit`）を検出する互換性バリデーションを追加
- 選択した親フォルダ配下のJARをサブフォルダまで再帰スキャンするように対応
- スキャン結果に親フォルダからのJAR相対パスを保持し、同名JARを別カテゴリでも安全に区別
- 元JARの相対フォルダ構造をlang入出力側にも保持
- 抽出済みの翻訳ターゲット（既定 `ja_jp`）は、JAR側のキー順・空白・改行をテンプレートとして同一キーの既存翻訳値だけを維持するマージ処理を追加
- JSONとlegacy `.lang` のマージ時に、新規キーはJAR側から追加、JARから削除された旧キーは出力から削除するように対応

### Changed
- 「JARへ反映」の内部処理を `ZipArchiveMode.Update` から `ZipArchiveMode.Create` による安全な一時アーカイブ再構築方式へ全面刷新（.NET 8で空エントリをSTORED化する際にData Descriptor bitが残存し、JavaでZipExceptionとなる不具合を解消）
- 画面表示とアセンブリのバージョンを 2.3.0 へ更新
- 「親フォルダと同じ場所をlang入出力ルートにする」がONの場合、`_lang_output` を挟まず親フォルダそのものを入出力ルートとして使用
- `_backup`、旧既定の `_lang_output`、指定した出力ルート配下とreparse pointを再帰JAR探索から除外
- FileSystemWatcherをサブフォルダ監視に対応し、除外フォルダ内の変更は無視
- 実行直前にJAR一覧を再列挙し、スキャン後に追加されたJARも再スキャン要求として検出
- スキャン一覧は同名JARを識別できるよう相対パス表示に変更
- 抽出先に同名ファイルがある場合の `*.conflict.*` 生成を廃止。翻訳ターゲット以外はJAR内容で上書きし、翻訳ターゲットはキー単位マージする方式へ変更
- 旧方式で生成済みの `*.conflict.*.json` / `*.conflict.*.lang` は次回抽出時に出力フォルダ直下から自動削除
- 抽出処理はスキャンで確定した `.json` / `.lang` のみを対象とし、lang直下の無関係な補助ファイルをコピーしないように変更

## v2.2.0 - 2026-09-03
### Added
- 外部で編集したlangファイルを、対応するJARの `assets/<modid>/lang/` へ追加・更新する「JARへ反映」機能を追加
- 単一modid／複数modidの既存出力構造を逆変換し、複数JARをまとめて反映
- 同一内容は無変更、未存在エントリは追加、内容が異なる既存エントリは更新として集計
- JARの一時コピーを更新・検証してから元ファイルと置換する安全な更新処理を追加
- 標準的な署名ファイルを含むJARを検出し、確認画面とログに警告を表示
- 抽出時に生成された `*.conflict.*` ファイルをJAR反映対象から自動除外

### Changed
- 実行ボタンを「langを抽出」と「JARへ反映」に分離
- 出力ルートの表記を「lang入出力ルート」に変更
- 画面表示とアセンブリのバージョンを2.2.0へ更新
- ファイルのシンボリックリンクもlang入出力対象から除外し、リンク非追従を徹底

## v2.1.2 - 2026-02-25
### Changed
- 抽出先の `lang` フォルダ生成を廃止し、lang配下のファイルを直接出力するように変更
- 1つのjarにlang候補が1つだけの場合は `modid` フォルダも作らず、`<出力先>/<jar名>/` 直下へ出力するように変更
- 1つのjarにlang候補が複数ある場合は `modid` フォルダを維持し、`<出力先>/<jar名>/<modid>/` 直下へ出力するように変更
- スキャン時の事前計画表示（CreateDir/Copy/Conflict）と実行時ログの出力先表示を新ルールに統一

## v2.1.1 - 2026-02-25
### Fixed
- 1つのjar内に複数のlangルートがある場合でも、抽出結果をjar単位でまとめて保持するように修正
- 抽出先パスから `assets` 階層を除外し、<出力先>/<jar名>/<modid>/lang/... で出力するように修正
- スキャン時の事前計画表示（CreateDir/Copy/Conflict）も新しい出力先ルールに統一

## v2.1.0 - 2026-02-25
### Added
- langフォールバック機能を追加（ターゲットファイルが無い場合、ソースからコピーして自動生成）。
  - 例: `ja_jp.json` が存在しなければ `en_us.json` からコピーして生成。
  - コピー元/コピー先のファイル名（拡張子なし）をオプションで自由に変更可能。
  - 拡張子はソースファイルのものをそのまま保持。
- Options / AppSettings に `LangFallbackEnabled`, `LangFallbackSourceName`, `LangFallbackTargetName` を追加。
- Executor に `ApplyLangFallback` メソッドを追加。
- PlannedOperationType に `FallbackCopy` を追加。
- MainWindow.xaml のオプション領域に「langフォールバック」セクションを追加。
- 設定は `settings.json` に永続化され、次回起動時に復元。

## v1.0.1 - 2026-02-23
### Fixed
- スキャン中にログ更新でクラッシュする問題を修正（`Logger` の `ObservableCollection` 更新を UI スレッドに統一）。

### Docs
- バグ詳細ログ `docs/logs/BUG-0001.md` を追加。
- 報告フロー文書 `docs/バグ・エラー報告フロー.md` を追加。
- `進捗.md` の「バグ / エラー修正ログ」に本件を追記。
