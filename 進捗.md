# 進捗
## 全体計画（設計書.md準拠）
1. WPF(MVVM)プロジェクト作成、基本画面（フォルダ選択/オプション/一覧/ログ）
2. スキャン（Mod列挙、assets有無、lang候補検出、方針/件数概算）
3. 実行計画（Move/Delete/CreateDir）とドライラン（予定操作の一覧表示）
4. 実行処理（確認ダイアログ、削除方式、バックアップZip、キャンセル/進捗、結果サマリ）
5. ログ保存（run-log.txt等）
6. ビルド/動作確認、README更新（必要なら）

## 現在の立ち位置
- フェーズ：v3.0.1 大容量言語ファイルのJAR反映ストリーム比較不具合修正
- いまやったこと：DeflateStream分割読み取り（Partial Read）への対応、ReadFullChunk導入、大容量言語ファイル回帰テスト追加
- 次にやること：配布パッケージング / 実機運用

## 完了したタスク
- 2026-09-07: v3.0.1 大容量言語ファイルのJAR反映ストリーム比較不具合修正
  - `JarArchiveUpdater` および `FileSystemService` において、`Stream.Read` がバッファサイズ未満を返す仕様（Partial Read）により、大容量ファイル（数万バイト〜100KB超）の比較時に誤って不一致と判定され「反映後JARの検証に失敗しました」が発生する問題を根本解消
  - バッファを要求サイズまたはEOFまで確実に埋める `ReadFullChunk` を導入
  - 100KB超の言語ファイルを用いた回帰テスト（`R6.LargeLangFileJarArchiveUpdate`）を追加
  - 実環境の全8MOD（BetterEnd, Incendium, Jadens-Nether-Expansion等）に対するシミュレーション検証完了
- 2026-09-05: v3.0.0 2ペイン・ダッシュボード型ワークスペースへのUI全面刷新
- 2026-09-04: v2.3.0 リソースパック出力追加 & Java互換性向上
  - 翻訳リソースパック生成機能 (`ResourcePackBuilder`) の追加（MOD JARを変更しない安全な翻訳適用）
  - UIに「リソースパック出力（推奨）」ボタンを追加し、出力先として `resourcepacks` フォルダを自動提案
  - `JarArchiveUpdater` の処理を `ZipArchiveMode.Update` から `ZipArchiveMode.Create` による一時アーカイブ再構築方式へ刷新
  - .NET 8 で空エントリが STORED 化された際に Data Descriptor bit が残り、Java で `ZipException: only DEFLATED entries can have EXT descriptor` が発生する問題を根本解消
  - 署名付きJAR（`META-INF/*.SF` 等）において署名対象ファイルの改変を安全にブロックし、`SecurityException` を防止
  - JAR出力直後にローカルファイルヘッダーを検査し、Java互換性を保証するバリデーションを追加
- 2026-09-03: JAR反映機能追加
  - 単一modidは `<OutputRoot>/<jar名>/`、複数modidは `<OutputRoot>/<jar名>/<modid>/` から反映
  - 未存在エントリは追加、内容が異なる既存エントリは更新、同一内容は無変更
  - 一時コピーの更新・内容検証後に元JARと置換し、途中失敗時は元JARを保持
  - 署名付きJARを検出し、確認画面とログで警告
  - UIを「langを抽出」「JARへ反映」の2操作に分離
- 2026-02-25: langフォールバック機能追加（DD Type B: 設計書v1.3に未記載の拡張）
  - 概要: lang抽出後、ターゲットファイル（例: ja_jp.json）が存在しなければソースファイル（例: en_us.json）からコピーして自動生成
  - Options/AppSettings: LangFallbackEnabled, LangFallbackSourceName, LangFallbackTargetName を追加
  - Executor: ApplyLangFallback メソッド追加（出力lang直下で拡張子を保持したまま名前だけ変換してコピー）
  - Enums: PlannedOperationType に FallbackCopy 追加
  - MainViewModel: 3プロパティ追加 + 設定永続化 + 実行確認ダイアログにフォールバック情報表示
  - MainWindow.xaml: オプション領域に「langフォールバック」セクション追加（有効/無効チェック + ソース/ターゲット名テキスト入力）
  - ビルド成功（警告0）
- 2026-02-24: 実行ステータス正確化 + 無効パス明示メッセージ + 設定保存の分離・安全化
  - 実行ステータス: ExecutionProgress モデル分離、Executor が Started/Completed + FinalStatus を返すよう変更（Skip/Warning/Fail がUIに正しく反映）
  - 無効パス: ScanCommand の有効条件を Directory.Exists 連動にし、実行時も無効パスなら明示メッセージ表示
  - 設定保存: SettingsService に分離（デバウンス＋直列化）、SettingsStore でアトミック保存（.tmp→Replace/Move）、破損JSON時は settings.broken.*.json 退避
  - 終了時: MainViewModel.Dispose() で設定フラッシュ
  - .gitignore: アプリケーション/ と *_wpftmp.csproj を除外に追加
  - 設計書.md: v1.0.1 実装反映
- 2026-02-23: スキャン中クラッシュ（Logs更新のスレッド違反）を修正し、BUGログを追加
  - 修正: Logger の ObservableCollection 更新を Dispatcher 経由に統一（UIスレッド実行）
  - 詳細: docs/logs/BUG-0001.md
- 2026-02-24: 設計書v1.3準拠で ModLangOrganizer プロジェクトとして全面再実装
  - Models層: Enums, JarScanResult, JarSnapshot, ExecutionPlan, ExecutionResult, LogEntry, Options
  - Infrastructure層: FileSystemService (リンク非追従), ArchiveExtractor (Zip Slip対策), Logger
  - Domain層: JarScanner (アーカイブ内エントリ解析), ConflictResolver, SnapshotValidator, Executor
  - MVVM基盤: ObservableObject, RelayCommand, AsyncRelayCommand
  - ViewModel層: MainViewModel (全コマンド/FileSystemWatcher), ModItemViewModel
  - Converters: BoolToVisibility, InverseBool, StatusToColor, LogLevelToColor, IntegrityToColor, SnapshotStateToColor, EnumToBool
  - UI層: モダンダークテーマ (Theme.xaml) + MainWindow.xaml (カード型レイアウト、カスタムDataGrid、ログビュー)
  - ビルド成功 (警告0)
- 2025-12-19: READMEの機能説明を現状仕様（assets外lang/個別抽出/jar出力先/設定永続化）に合わせて更新
- 2025-12-19: 起動時例外（設定復元時のNRE）を修正し、コマンド初期化後に設定ロードするように変更
- 2025-12-19: 設定（オプション/TargetDir）を settings.json に保存し、起動時に復元するように対応
- 2025-12-19: jarモードの抽出先を _jar_lang から jarと同フォルダ直下（<jar名>/lang）へ変更
- 2025-12-19: lang検出を拡張（assets外のlangも対象）し、複数langを個別抽出できるモードを追加
- 2025-12-18: 設計書.mdを確認し、進捗.mdを新規作成
- 2025-12-18: WPF(MVVM)プロジェクト作成（スキャン/実行/ドライラン/ログ/安全確認/キャンセル/バックアップZip/ゴミ箱削除）を実装
- 2025-12-18: dotnet build (Release) でビルド成功を確認
- 2025-12-18: publish.ps1追加、READMEに配布（self-contained/framework-dependent）手順を追記
- 2025-12-18: 起動失敗（ProgressBarのTwoWayバインド問題）を修正し、起動時例外ログ出力を追加
- 2025-12-18: 実行時エラー（Logs更新のスレッド問題）を修正
- 2025-12-19: 進捗管理/Git運用ルール（開発運用ルール.md）を追加し、v1.1（jarモード）対応に着手
- 2025-12-19: jarモード（.jarのままassets/<modid>/lang抽出）を実装し、dotnet build (Release) でビルド成功を確認
- 2025-12-19: publish.ps1 に both モードを追加し、同梱/非同梱を別フォルダへ出力できるように整理
- 2025-12-19: publish.ps1 に -Zip を追加し、同梱/非同梱の配布用zip（識別しやすいファイル名）を自動生成

## バグ / エラー修正ログ
- 2026-02-23: スキャン中クラッシュ（UIスレッド外からの Logs 更新）
        - 現象：スキャン実行中にアプリがクラッシュする
        - 発生条件：バックグラウンド処理中（Task.Run）にログを連続追加
        - 原因：UIスレッド以外から ObservableCollection（LogEntries）を更新
        - 対策：Logger で Dispatcher を保持し、Add/Clear を UI スレッドへディスパッチ
        - 詳細ログ：docs/logs/BUG-0001.md
- 2025-12-19: 起動時例外（設定復元でNullReferenceException）
        - 現象：アプリを閉じて再起動すると Startup failed で落ちる
        - 発生条件：設定永続化を有効にした状態で再起動
        - 原因：MainViewModelの設定復元がコマンド初期化前に走り、Setter内の RaiseCanExecuteAll で null を参照
        - 対策：コマンド初期化後に設定ロードする順序に変更し、RaiseCanExecuteAll を null 安全化
- 2025-12-18: 起動失敗（ProgressBar TwoWayバインド）
        - 現象：アプリ起動直後に「ProgressValue が読み取り専用のため TwoWay バインド不可」で例外終了
        - 発生条件：ProgressBar.Value を既定の TwoWay バインドで読み取り専用プロパティへバインド
        - 原因：WPF の ProgressBar.Value は既定で TwoWay、VM 側 ProgressValue が private set
        - 対策：ProgressBar/ProgressText を OneWay バインドに変更、起動時例外を crash-log.txt に出力
- 2025-12-18: 実行時エラー（ItemsControl が項目ソースと一致していません）
        - 現象：実行中にログDataGridが例外（ItemsControl/ItemContainerGenerator の不整合）で落ちる
        - 発生条件：実行処理がバックグラウンドで動作中に Logs(ObservableCollection) を更新
        - 原因：UIスレッド以外から ObservableCollection を更新し、DataGrid が CollectionChanged の整合性を保てない
        - 対策：Logger が Dispatcher 経由で UI スレッドへログ追加するように修正

## 未着手 / 保留
- [ ] 単体テスト
- [ ] E2Eテスト（実jarでの動作確認）
- [ ] 配布用パッケージング（self-contained / framework-dependent）

## メモ / 課題
- ドライラン時の「予定操作一覧」は、追加画面を増やさずログ領域で表示する方針（設計書の単一ウィンドウ要件優先）。
- v2.0で旧OrganizerToolプロジェクトを ModLangOrganizer に完全リプレース（モダンダークテーマUI、設計書v1.3フル準拠）。
